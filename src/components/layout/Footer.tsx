import React from "react"

interface IFooterProps {

}

export const Footer: React.FC<IFooterProps> = React.memo(() => {
    return (
        <div className="le-layout-footer">

        </div>
    )
})