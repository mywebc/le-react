import React from "react"

interface IHeaderProps {

}

export const Header: React.FC<IHeaderProps> = React.memo(() => {
    return (
        <div className="le-layout-header">

        </div>
    )
})